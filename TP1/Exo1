Option Explicit
Private Sub Exo1()

Dim n%
Dim af$
Dim ta$

n = CInt(InputBox("Texte", "Saisie"))

n = n * 2%

af = " Le resultat est " + Format(n, "0.000")

Call MsgBox(af, vbInformation, "Resultat de l'operation")



End Sub
